# Put this in your frontend project root as .env.local
VITE_N8N_BASE_URL=http://localhost:5678
# For testing webhooks use /webhook-test; for production use /webhook
VITE_N8N_ENDPOINT=/webhook-test/etl-ibmi-kpis
# Optional, only if your n8n webhook is protected
# VITE_N8N_API_KEY=your_api_key_here
